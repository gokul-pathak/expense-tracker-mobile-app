# Public Expo client configuration. These are identifiers, not secrets.
# Leave them unset to run the app in Local Only mode.
#
# Never add a service-role key, a database password or an admin token here:
# anything in this file is bundled into the app.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
