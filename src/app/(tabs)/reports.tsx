import { PlaceholderScreen } from '@/components/ui';

export default function ReportsScreen() {
  return (
    <PlaceholderScreen
      title="Reports"
      description="Weekly, monthly, yearly, category and savings reports come after the core engine is reliable."
      milestone="M5 Reports"
    />
  );
}
